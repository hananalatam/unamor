[build]
  command = "npm install terser && npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  framework = "astro"

# Redirección para procesar los tokens de confirmación
[[redirects]]
  from = "/#confirmation_token=*"
  to = "/confirm/"
  status = 302
  force = true

[[redirects]]
  from = "/?confirmation_token=*"
  to = "/confirm/"
  status = 302
  force = true

# Redirección para el resto de las páginas
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200